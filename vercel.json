{
  "framework": "nextjs",
  "installCommand": "npm install --no-audit --no-fund",
  "buildCommand": "npm run build",
  "functions": {
    "api/**": {
      "runtime": "nodejs18.x",
      "maxDuration": 60
    },
    "app/**": {
      "runtime": "nodejs18.x",
      "maxDuration": 60
    }
  }
}
