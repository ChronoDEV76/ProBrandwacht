#!/bin/sh

node scripts/tone/tone-guard-autofix.mjs --dry --check

echo "🔍 Running Tone of Voice check..."
npm run tone-check

status=$?

if [ $status -ne 0 ]; then
  echo "⚠️  Tone check heeft issues gevonden. Fix de toon voordat je commit."
  exit $status
fi

echo "✅ Tone check geslaagd. Commit gaat door."
